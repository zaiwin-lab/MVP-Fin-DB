# ── Bigcapital backbone ────────────────────────────────────────────
DB_USER=bigcapital
DB_PASSWORD=change-me-strong-password
JWT_SECRET=change-me-long-random-string

# Outbound email — invoices, receipts, payment reminders
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=postmaster@example.com
MAIL_PASSWORD=change-me
MAIL_FROM_ADDRESS=finance@yourbusiness.com
MAIL_FROM_NAME=YourBusiness Finance

# ── AI CFO layer (ai-cfo/) ─────────────────────────────────────────
# Get a key from https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Bigcapital API access for the CFO agent.
# Create a token in the Bigcapital UI: Settings → API / Access tokens.
BIGCAPITAL_API_URL=http://localhost:3000/api
BIGCAPITAL_API_TOKEN=change-me
